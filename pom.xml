<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.architonix.sonarqube</groupId>
  <artifactId>sonar-offline-report-plugin</artifactId>
  <version>2.0.1</version>
  <packaging>sonar-plugin</packaging>

  <name>SonarQube Offline Report Plugin</name>
  <description>Generate self-contained HTML, XLSX, CSV, DOCX and print-ready single-project or portfolio reports from SonarQube Community Build.</description>
  <url>https://github.com/architonixlabs/sonar-offline-report-plugin</url>

  <scm>
    <connection>scm:git:https://github.com/architonixlabs/sonar-offline-report-plugin.git</connection>
    <developerConnection>scm:git:https://github.com/architonixlabs/sonar-offline-report-plugin.git</developerConnection>
    <url>https://github.com/architonixlabs/sonar-offline-report-plugin</url>
    <tag>v2.0.1</tag>
  </scm>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>11</maven.compiler.release>
    <sonar.plugin.api.version>13.7.0.4381</sonar.plugin.api.version>
    <maven.enforcer.version>3.6.3</maven.enforcer.version>
    <maven.clean.version>3.2.0</maven.clean.version>
    <maven.resources.version>3.5.0</maven.resources.version>
    <maven.install.version>3.1.4</maven.install.version>
    <maven.deploy.version>3.1.4</maven.deploy.version>
    <cyclonedx.maven.version>2.9.3</cyclonedx.maven.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.sonarsource.api.plugin</groupId>
      <artifactId>sonar-plugin-api</artifactId>
      <version>${sonar.plugin.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.25.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <directory>${project.basedir}</directory>
        <targetPath>META-INF</targetPath>
        <includes>
          <include>LICENSE</include>
          <include>NOTICE</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-clean-plugin</artifactId>
        <version>${maven.clean.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resources.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-install-plugin</artifactId>
        <version>${maven.install.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${maven.deploy.version}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.version}</version>
        <executions>
          <execution>
            <id>enforce-build-environment</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[17,)</version>
                </requireJavaVersion>
                <requireMavenVersion>
                  <version>[3.9.0,4.0.0)</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.sonarsource.sonar-packaging-maven-plugin</groupId>
        <artifactId>sonar-packaging-maven-plugin</artifactId>
        <version>1.23.0.740</version>
        <extensions>true</extensions>
        <configuration>
          <pluginKey>offlinereport</pluginKey>
          <pluginClass>com.architonix.sonarqube.offlinereport.OfflineReportPlugin</pluginClass>
          <pluginApiMinVersion>13.7.0.4381</pluginApiMinVersion>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>11</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
      </plugin>
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>${cyclonedx.maven.version}</version>
        <executions>
          <execution>
            <id>generate-release-sbom</id>
            <phase>verify</phase>
            <goals>
              <goal>makeBom</goal>
            </goals>
            <configuration>
              <outputFormat>json</outputFormat>
              <outputName>${project.artifactId}-${project.version}.cdx</outputName>
              <includeBomSerialNumber>false</includeBomSerialNumber>
              <includeTestScope>false</includeTestScope>
              <includeLicenseText>false</includeLicenseText>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
